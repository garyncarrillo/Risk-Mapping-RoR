<p id="notice"><%= notice %></p>

<h1>Paquete Plans</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Año</th>
      <th>Alcance</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @paquete_plans.each do |paquete_plan| %>
      <tr>
        <td><%= paquete_plan.nombre %></td>
        <td><%= paquete_plan.ano %></td>
        <td><%= paquete_plan.alcance %></td>
        <td><%= link_to 'Mostrar', paquete_plan %></td>
        <td><%= link_to 'Editar', edit_paquete_plan_path(paquete_plan) %></td>
        <td><%= link_to 'Eliminar', paquete_plan, method: :delete, data: { confirm: 'Estas seguro que deseas Eliminar?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Paquete', new_paquete_plan_path %>
