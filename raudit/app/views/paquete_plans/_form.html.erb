<%= form_with(model: paquete_plan, local: true) do |form| %>
  <% if paquete_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(paquete_plan.errors.count, "error") %> prohibited this paquete_plan from being saved:</h2>

      <ul>
      <% paquete_plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :ano %>
    <%= form.text_field :ano %>
  </div>

  <div class="field">
    <%= form.label :alcance %>
    <%= form.text_area :alcance %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
