<%= form_with(model: centro, local: true) do |form| %>
  <% if centro.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(centro.errors.count, "error") %> prohibited this centro from being saved:</h2>

      <ul>
      <% centro.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :estados_id %>
    <%= form.collection_select(:estados_id, Estado.all, :id, :descripcion) %>
  </div>

  <div class="actions">
    <%= form.submit  class: "btn btn-primary"%>
  </div>
<% end %>
