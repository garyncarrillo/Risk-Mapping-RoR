<%= form_with(model: proceso, local: true) do |form| %>
  <% if proceso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(proceso.errors.count, "error") %> prohibited this proceso from being saved:</h2>

      <ul>
      <% proceso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :objetivo %>
    <%= form.text_area :objetivo %>
  </div>

  <div class="field">
    <%= form.label :tipo_objetivo %>
    <%= form.collection_select(:tipo_objetivo, TipoObjetivo.all, :id, :descripcion) %>
  </div>

  <div class="field">
    <%= form.label :tipo %>
    
    <%= form.collection_select(:tipo, TipoProceso.all, :id, :nombre) %>
  </div>

  <div class="field">
    <%= form.label :centro %>
    <%= form.collection_select(:centro, Centro.all, :id, :nombre) %>
  </div>

  <div class="field">
    <%= form.label :estado %>
    <%= form.collection_select(:estado, Estado.all, :id, :descripcion) %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
