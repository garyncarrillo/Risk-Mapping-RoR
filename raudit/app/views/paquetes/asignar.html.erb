<!DOCTYPE html>
<html>
<head>
	<title></title>
	
</head>
<script type="text/javascript">
	$(document).ready(function() {
			load();
			$( "#agregar" ).click(function() {	
				var Items = document.getElementById("lista_procedimientos").value
				//alert(document.getElementById("glista").value);
				//alert('Blanco'+Items.trim());
				
				$("#drop").append(" <option>"+Items+" </option>");
				
			});

			$( "#quitar" ).click(function() {	
				if($("#drop option:selected").value=== "undefined"){
					alert("Debes seleccionar");
				}else{
					alert($("#drop option:selected").value);
					$("#drop option:selected").remove();	
				}
				
			});


			function load(){
					load_paquetes();
					load_procesos();
					load_procedimientos();
			}

			function load_paquetes(){
		 		
			   $.ajax({
	                data:  { 
     			   		 'foo': 'bar', 
        		  		 'foot': 'bares' 
    				},
	                url:   '/api/getpaquetes', //archivo que recibe la peticion
	                type:  'get', //método de envio
	                dataType: "json",
	                beforeSend: function () {
	                        //alert('cargando informacion');
	                },
	                success:  function (response) { //una vez que el archivo recibe el request lo procesa y lo devuelve
	                        for(i=0;i<response.back_end.length;i++){
	                        		var Items = response.back_end[i]['nombre'];
	                        		//alert(Items);
	                        		$("#lista_paquetes").append(" <option>"+Items+" </option>");
							}
	                },
	                error:  function (data) { //una vez que el archivo recibe el request lo procesa y lo devuelve
	                        alert("Error"+data);
	                }
	        	});
			}


			function load_procesos(){
			   $.ajax({
	                data:  { 
     			   		 'foo': 'bar', 
        		  		 'foot': 'bares' 
    				},
	                url:   '/api/getproceso', //archivo que recibe la peticion
	                type:  'get', //método de envio
	                dataType: "json",
	                beforeSend: function () {
	                        //alert('cargando informacion');
	                },
	                success:  function (response) { //una vez que el archivo recibe el request lo procesa y lo devuelve
	                        for(i=0;i<response.back_end.length;i++){
	                        		var Items = response.back_end[i]['nombre'];
	                        		//alert(Items);
	                        		$("#lista_procesos").append(" <option>"+Items+" </option>");
							}
	                },
	                error:  function (data) { //una vez que el archivo recibe el request lo procesa y lo devuelve
	                        alert("Error"+data);
	                }
	        	});
			}

			function load_procedimientos(){
			   $.ajax({
	                data:  { 
     			   		 'foo': 'bar', 
        		  		 'foot': 'bares' 
    				},
	                url:   '/api/getprocedimientos', //archivo que recibe la peticion
	                type:  'get', //método de envio
	                dataType: "json",
	                beforeSend: function () {
	                        //alert('cargando informacion');
	                },
	                success:  function (response) { //una vez que el archivo recibe el request lo procesa y lo devuelve
	                        for(i=0;i<response.back_end.length;i++){
	                        		var Items = response.back_end[i]['nombre'];
	                        		//alert(Items);
	                        		$("#lista_procedimientos").append(" <option>"+Items+" </option>");
							}
	                },
	                error:  function (data) { //una vez que el archivo recibe el request lo procesa y lo devuelve
	                        alert("Error"+data);
	                }
	        	});
			}

	});



</script>		
<body>
	<h1> Asignar Recursos a los paquetes </h1>
	<br></br>

	<label style="margin-left: 15%;" >Paquetes</label>
	<input list="lista_paquetes" name="" style="margin-left: 15%; width: 30%;">
	<datalist id="lista_paquetes">
		<option></option>
	</datalist>

	<label style="margin-left: 15%;" >Procesos</label>
	<input  name="lista_procesos" list="lista_procesos" style="margin-left: 15%; width: 30%;" />
	<datalist id="lista_procesos">
		<option></option>
	</datalist>



	<label style="margin-left: 15%;" >Procedimientos</label>
	<input  id="lista_procedimientos" list="lista_procedimientos" style="margin-left: 15%; width: 30%;" />
	<datalist id="lista_procedimientos">
		<option></option>
	</datalist>


	<br></br>
	<input type="button" id="agregar" name="" value="Agregar>>" style="width: 10%; margin-left: 20%;">
	<br></br>
        <select name="drop" id="drop" size="6" style="margin-left: 15%;">
        </select>

	<br></br>
	<input type="button" id="quitar" name="quitar" value="quitar" style="width: 10%; margin-left: 60%;">
	<br></br>
	<input type="button" id="guardar" name="" value="Guardar" style="width: 10%; margin-left: 5%;">
</body>
</html>
