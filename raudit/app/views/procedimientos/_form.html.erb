<%= form_with(model: procedimiento, local: true) do |form| %>
  <% if procedimiento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(procedimiento.errors.count, "error") %> prohibited this procedimiento from being saved:</h2>

      <ul>
      <% procedimiento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :objetivo %>
    <%= form.text_area :objetivo %>
  </div>

  <div class="field">
    <%= form.label :tipo_objetivo %>
   <%= form.collection_select(:tipo_objetivo, TipoObjetivo.all, :id, :descripcion) %>
  </div>

  <div class="field">
    <%= form.label :Proceso %>
    <%= form.collection_select(:idprocesos, Proceso.all, :id, :nombre) %>
  </div>

  <div class="field">
    <%= form.label :cargo_lider %>
    <%= form.text_field :cargo_lider %>
  </div>

  <div class="actions">
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>
